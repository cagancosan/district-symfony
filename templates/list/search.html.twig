{% extends 'base.html.twig' %}
{% block title %}
	The District - Accueil
{% endblock %}
{% block body %}
{% embed 'components/component.searchBar.html.twig' %}{% endembed %}
	<div class="mb-8 hidden text-6xl text-visible lg:block">Les catégories '{{ search }}'</div>
	<div class="hidden w-full grid-cols-1 place-items-center gap-8 sm:grid-cols-2 lg:grid lg:grid-cols-3">
		{% for categorie in categories %}
			{% embed 'components/component.cardCategorie.html.twig' with { categorie: categorie } %}{% endembed %}
		{% endfor %}
	</div>
	<div class="my-8 text-6xl text-visible">Les plats '{{ search }}'</div>
	<div class="grid w-full grid-cols-1 place-items-center gap-8 xl:grid-cols-2">
		{% for food in foods %}
				{% embed 'components/component.cardFood.html.twig' with { food: food } %}{% endembed %}
		{% endfor %}
	</div>
{% endblock %}
